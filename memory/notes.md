# Portfolio Notes

For active work, use `memory/BACKLOG.md` as the main todo list. This file keeps short working notes that do not yet deserve a rule or backlog item.

## Current Reminders

- `memory/CURRENT_STATE.md` is the preferred first-read project memory.
- `memory/PROJECT_MEMORY.md` is historical archive and may contain old decisions.
- `memory/agent-memory/*.md` is historical only unless the user explicitly revives the old multi-agent split.

## Asset Notes

- A stronger aitryon cover image is still needed.
- Aitryon process visuals are still needed: information architecture, generation flow, SEO system, or decision diagrams.
- Metric evidence or source notes are still needed if the case is used for serious hiring review.
- Some assets look duplicated by size; run `node tools/check-links-assets.mjs` before cleanup.

## Language Notes

- Language toggle is already implemented with `data-i18n`, JavaScript translations, and `localStorage`.
- Future visible translatable copy should be added in both English and Chinese.
- The project can keep the current single-page toggle unless a future SEO/localization need justifies `/zh/` and `/en/`.
